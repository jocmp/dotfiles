if [ "$(uname)" != "Darwin" ]; then
  alias pbcopy='clip.exe'
  alias pbpaste="powershell.exe -command 'Get-Clipboard' | sed -e 's/\r\n$//g'"
  alias fd="fdfind"
else
  alias pip=pip3
fi

alias ls="exa"
alias n="nano"
alias ms="mosh"
